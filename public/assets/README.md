# public/assets

Drop `surfer.mp4` (looping surf footage, ~5-10s, H.264 MP4 < 3 MB ideal) here.

It's the background video for `app/v2/components/LoadingScreen.jsx`. If the
file is missing the LoadingScreen still renders (solid ocean-blue fallback
behind the veil + wordmark) — the app never breaks.
