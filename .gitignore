node_modules
.next
.env*.local
.DS_Store
*.log

# Build artifact — regenerated by scripts/gen-version.mjs on every build.
# Keeping it out of git kills the "git restore before add" ritual and the
# risk of committing a stale version stamp.
public/version.json

# Vercel project link (contains projectId/orgId)
.vercel

# Bash whitelist API test artifacts (curl outputs from .claude/settings.local.json)
caps.xml
anmn.json
nrt_wave.json
icon_s.json
e1.json
e2.json
erddap.json
